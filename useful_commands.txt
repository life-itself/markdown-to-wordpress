//Commands
python test_simple.py
python -m src.cli validate -i sample-data/content
python -m src.cli inspect -f sample-data/content/about.md

python etl/run_pipeline.py --test 0-image-processing
python etl/run_pipeline.py --test 1-prepare-content
*... other ETL python scripts with their folder names ...*

// Unit Tests
python -m pytest tests/ -v
python -m pytest tests/ --cov=src --cov-report=term-missing

task:
- fetch obisidan links [brackets] to be full links


🎉 Perfect! WordPress Post Creation is
  Now Implemented!

  To Answer Your Question:

  Where will the WordPress posts be
  created?

  1. WordPress Admin Dashboard:
    - https://your-wordpress-site.com/wp-
  admin/
    - Go to Posts → All Posts (for blog
  posts)
    - Go to Pages → All Pages (for pages)
  2. Front-end URLs:
    -
  https://your-wordpress-site.com/about/
  (about page)
    - https://your-wordpress-site.com/com
  munity/ (community blog post)
    -
  https://your-wordpress-site.com/events/
  conscious-coliving-retreat-spring-2024/
   (event)

  How to Actually Create the Posts:

  cd python-migration

  # Test first (dry run)
  python -m src.cli migrate -i
  sample-data/content --wp-url
  https://your-site.com --wp-user admin
  --wp-password your-app-password
  --dry-run

  # Actually create posts (remove
  --dry-run)
  python -m src.cli migrate -i
  sample-data/content --wp-url
  https://your-site.com --wp-user admin
  --wp-password your-app-password

  What You Need:

  1. A WordPress website
  2. Application password from WordPress
  Admin → Users → Profile → Application
  Passwords
  3. Run the command above with your real
   WordPress URL and credentials

  The tool will create 3 WordPress
  posts/pages from your sample markdown
  files and show you the WordPress IDs
  where you can find them!

  Check out MIGRATION_GUIDE.md for the
  complete step-by-step instructions! 🚀